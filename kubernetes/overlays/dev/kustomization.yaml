apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rag-dev

resources:
  - ../../base

namePrefix: dev-

commonLabels:
  environment: dev

patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target:
      kind: Deployment
      name: rag-platform
  - patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: rag-dev.example.com
      - op: replace
        path: /spec/tls/0/hosts/0
        value: rag-dev.example.com
    target:
      kind: Ingress
      name: rag-platform

images:
  - name: ghcr.io/your-org/rag-platform
    newTag: dev
