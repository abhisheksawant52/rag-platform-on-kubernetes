apiVersion: v1
kind: Service
metadata:
  name: rag-platform
  labels:
    app: rag-platform
    app.kubernetes.io/name: rag-platform
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app: rag-platform
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
