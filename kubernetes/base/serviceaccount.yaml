apiVersion: v1
kind: ServiceAccount
metadata:
  name: rag-platform
  labels:
    app: rag-platform
  annotations:
    # Enable Workload Identity / IRSA when using AWS EKS or GKE:
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/rag-platform-role
automountServiceAccountToken: false
